# import pymongo
#
# host = 'localhost'
# port = 27017
#
# # build connection with DB
# client = pymongo.MongoClient(host, port)
#
# # select 'hash_tag_investor' collection
# dbConnection = client.hash_tag_investor
